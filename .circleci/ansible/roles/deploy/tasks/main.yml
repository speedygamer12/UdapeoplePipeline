---
- name: "extract archived artifact"
  become: true
  unarchive:
    src: ~/project/.circleci/artifact.tar.gz
    dest: ~/

- name: "start app"
  shell: |
    cd artifact
    npm install 
    pm2 stop default 
    pm2 start npm -- start